# Docker (manimcommunity/manim) already ships the `manim` CLI — do not install the PyPI `manim`
# package in those images (avoids version clashes and redundant heavy deps).
# paddlepaddle: 2.6.1 wheels are no longer published on PyPI for many platforms; use 2.6.2+.

fastapi>=0.115.0,<1
uvicorn[standard]>=0.30.0
sympy>=1.12
# NumPy 1.26+ needs Python 3.9+; manimcommunity/manim:v0.18.0 uses Python 3.8 (numpy max 1.24.x on PyPI).
# OpenCV / Paddle stacks stay on NumPy 1.x ABI.
numpy>=1.24.0,<2
# scipy 1.11+ requires Python 3.9+
opencv-python-headless>=4.6,<4.10
scipy>=1.10.0,<2
pydantic[email]>=2.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
openai>=1.0.0
supabase>=2.0.0
celery>=5.3.0
redis>=5.0.0
websockets>=12.0
httpx>=0.27.0
pix2tex>=0.1.4
email-validator>=2.0.0
paddleocr==2.7.3
paddlepaddle==2.6.2
ultralytics==8.2.2
