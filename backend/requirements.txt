# Docker (manimcommunity/manim) already ships the `manim` CLI — do not install the PyPI `manim`
# package in those images (avoids version clashes and redundant heavy deps).
# paddlepaddle: 2.6.1 wheels are no longer published on PyPI for many platforms; use 2.6.2+.

fastapi>=0.115.0,<1
uvicorn[standard]>=0.30.0
sympy>=1.12
# OpenCV / Paddle / many scientific wheels still expect NumPy 1.x ABI
numpy>=1.26.0,<2
# Newer opencv headless pins numpy>=2; cap major series for numpy 1.x stacks
opencv-python-headless>=4.6,<4.10
scipy>=1.11.0
pydantic[email]>=2.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
openai>=1.0.0
supabase>=2.0.0
celery>=5.3.0
redis>=5.0.0
websockets>=12.0
httpx>=0.27.0
pix2tex>=0.1.4
email-validator>=2.0.0
paddleocr==2.7.3
paddlepaddle==2.6.2
ultralytics==8.2.2
