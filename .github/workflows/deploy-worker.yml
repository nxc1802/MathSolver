name: Deploy Worker to Hugging Face

on:
  push:
    branches: [ main ]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-worker.yml'

jobs:
  deploy-worker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Push to Hugging Face Spaces (Worker)
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git config --global user.email "cuong@example.com"
          git config --global user.name "Cuong2004"
          
          mkdir hf_worker_copy
          cp -r backend/* hf_worker_copy/
          
          cd hf_worker_copy
          
          # Crucial: HF expects 'Dockerfile' at the root. 
          # We rename our worker-specific Dockerfile to 'Dockerfile' for this Space.
          if [ -f "Dockerfile.worker" ]; then
            mv Dockerfile.worker Dockerfile
          fi
          
          git init
          git remote add origin https://Cuong2004:$HF_TOKEN@huggingface.co/spaces/Cuong2004/math-solver-worker
          git add .
          git commit -m "Deploy Worker from GitHub Actions"
          git push --force origin master:main
